package com.example.audition.domain.mento.repository;

import com.example.audition.domain.mento.Mento;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MentoRepository extends JpaRepository<Mento,String > {
}
